# Raw CSV Dataset

This folder contains all the raw CSV files for ingestion by the University Rankings pipeline. It also gives me a file with which to trigger the `azcopy` utility's GitHub Actions workflow by making arbitrary changes to this file, because that workflow is triggered on changes to the `raw` directory.

